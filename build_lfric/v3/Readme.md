# Build scripts for lfric_atm

You can build lfric_atm or any other app using the script

```bash
bash build_gcc.sh
```

You might need to modify some the environment variables at the top of the file.