# xios_trace

A tool to generate a perfetto trace from xios logs.
To produce a trace.json file with all the xios logs from a folder type

```bash
python xios_trace.py <folder>/xios_*.out --output trace.json
```

